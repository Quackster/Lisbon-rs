//! Mirrors `net.h4bbo.lisbon.game.commands.registered.SummonAllCommand`.

/// Mirrors `SummonAllCommand` (the Java class is empty and does not
/// extend `Command`; it is not registered with the `CommandManager`).
pub struct SummonAllCommand;
